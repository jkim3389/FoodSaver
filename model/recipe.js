export default class Recipe {
    constructor(id, title, imageUrl, usedIngredients) {
        this.id = id,
        this.title = title,
        this.imageUrl = imageUrl,
        this.usedIngredients = usedIngredients
    }
};
